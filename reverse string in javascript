//first method
function reverseString(str) {
var reversedStr = "";
  for (let i = str.length - 1; i >= 0; i--) {
    reversedStr += str[i];
  }
  return reversedStr;
}

//second method

function reverseString(str) {
  return str
    .split("")
    .reverse()
    .join("");
}
